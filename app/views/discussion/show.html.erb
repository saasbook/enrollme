<html>
<body>
  <title>Upload Discussion</title>
  
    <div class="container">
      
      <h1>Discussion Sections</h1>
      
      <div class="row">
        <div class="col-md-8">
          <h2>Members</h2>
          <table class="table table-striped table-hover">

            
            <!--<% Team.find(@team.id).users.each do |u| %>-->
            <!--  <tr>-->
            <!--    <td></td>-->
            <!--    <td><%= u.name %></td>-->
            <!--    <td><%= u.major == "Computer Science" or u.major == "Electrical Engineering and Computer Science" ? "•" : "No" %></td>-->
            <!--    <td>-->
            <!--      <%= form_tag edit_team_path(@team), method: :get do %>-->
            <!--        <%= hidden_field_tag :unwanted_user, u.id %>-->
            <!--        <%= submit_tag "Remove", {class: "btn btn-default", id: "remove_"+u.name, data: { confirm: "Are you sure you want to remove " +u.name+" from your team?" }}  %>-->
            <!--      <% end %>                  -->
            <!--    </td>-->
            <!--  </tr>-->
            <!--<% end %>-->
          </table>
          
        </div>
      </div>
      <div class="actions">
        <div class="col-xs-2">
          <%= form_tag edit_team_path(@team), method: :get do %>
            <%= hidden_field_tag :unwanted_user, @user.id %>
            <%= submit_tag "Leave team", {class: "btn btn-default", id: "leave_team"}  %>
          <% end %>
        </div>
        <% if @team.approved %>
          <div class="col-xs-2">
            Team has been approved!
          </div>
        <% elsif @team.submitted %>
          <div class="col-xs-2">
            <%= form_tag unsubmit_team_path do %>
              <%= submit_tag "Withdraw Team Submission", class: "btn btn-primary btn-xs" %>
              <% # TODO: css styling for this button so that it fits using white-space: normal;  %>
            <% end %>
          </div>
          <div class="col-xs-2">  
            Team has been submitted!
          </div>
        <% elsif @team.eligible? %>
          <div class="col-xs-2">
            <%= form_tag submit_team_path do %>
              <%= submit_tag "Submit team", class: "btn btn-primary" %>
            <% end %>
          </div>
          <div class="col-xs-2">  
            Warning: You need to submit your team to be considered for enrollment.
          </div>
        <% else %>
          <div class="col-xs-2">
            <%= submit_tag "Submit team", class: "btn btn-primary", disabled: true %>
          </div>
        <% end %>
      </div>
      
      <table class="table table-striped table-hover">
        <tr>
            <td></td>
            <td>Discussions</td>
            <td>Seats Open</td>
            <td></td>
          </tr>
          
        <% @discussions.each do |d| %>
          <tr>
            <td></td>
            <td><%= d.time %></td>
            <td><%= d.seats_open %></td>
          </tr>
        <% end %>
      </table>
    </div>
</body>
</html>