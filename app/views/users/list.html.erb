<table id="individuals">
  <tbody>
  <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= link_to "Request User", user_requests_path(user_id: session[:user_id], team_id: team.id), method: :post %></td>
      </tr>
  <% end %>
  </tbody>
</table>